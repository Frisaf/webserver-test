{% extends "layout.njk" %}

{% block content %}

<main class="container">
    <h1>{{ title }}</h1>
    <p>{{ movie.year }}, {{ movie.language }}, {{ movie.genre }}, <strong>{{ movie.rating}}</strong></p>
    <hr>
    <p>{{ movie.review }}</p>
</main>

{% endblock %}